import { MatDialogConfig } from '@angular/material/dialog';

export const fullScreenConfig: MatDialogConfig = {
    width: '100%',
    maxWidth: '100vw',
    height: '100%',
    maxHeight: '100vh',
}

export const bottomDialogConfig: MatDialogConfig = {
    width: '100%',
    maxWidth: '100vw',
}
